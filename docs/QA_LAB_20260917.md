# Real OPNsense lab qualification — 1.15.1 and 1.15.2 correction

## Environment and boundary

Two temporary OPNsense 26.1.2_5 VMs, 4 GiB RAM and 20 GiB disk each, restored from backup and sanitized before use. Three bridges have no physical uplinks or host addresses. Separate management, WAN client and LAN server namespaces have only lab routes. Scoped host firewall rules block routing between the lab and other networks. Production remains `read_only:true` throughout.

The real plugin handlers, HTTPS client and browser use the two real firewall APIs. The lab replaces only the PegaProx authentication boundary with an SSH-protected adapter; production authentication is covered separately by its authenticated read-only smoke. Lab credentials and machine details are kept outside this repository in the encrypted vault.

## Findings reproduced and corrected

1. Local apply/reconfigure does not trigger XMLRPC in 26.1.2. The original request saved on A and returned `409 ha_unverified`, correctly detecting absence on B. Explicit, opt-in `xmlrpc_pf` synchronization now performs the guarded native action and peer readback.
2. XMLRPC retains the final DNAT on B when deleting it removes A's entire `nat/rule` branch. The first deletion returned `409`, not a false success. Exact-UUID cleanup now requires matching initial revisions and single-rule inventories, unchanged peer contents and safe HA; absence is verified on both nodes.
3. DNAT search adds generated anti-lockout rows without increasing `total`. A truncated response could therefore look complete. Codex identified the case; a regression failed before the fix and passed after counting only real DNAT rows for completeness.

Lab setup corrections: permit CARP on WAN, regenerate cloned host identities, and enter both host mount and network namespaces when starting a VM from the test runner. These changes affect only the temporary lab.

## Observed verification

| Check | Result |
|---|---|
| Full automated suite, Chromium E2E enabled (1.15.1 run) | 426 passed, 19 skipped |
| Corrected suite, Chromium E2E enabled (1.15.2 run) | 435 passed, 19 skipped |
| Ruff on changed Python files; Semgrep diff | Passed; zero findings |
| Mixed CARP ownership | POST rejected `409 ha_unsafe`; no alias created |
| Real alias create/update/delete | Same UUID/content verified on both nodes |
| Sole editable alias deletion, 1.15.2 follow-up | Both inventories empty before create; one identical UUID on both; delete applied and verified; both inventories empty afterwards |
| Real filter and DNAT create/update/delete | Applied and verified on both nodes |
| Sole API filter rule deletion | Initial and final API filter inventories empty; delete applied and peer verified, direct traffic subsequently blocked |
| Stale revision / insufficient permission / upstream invalid address | 409 / 403 / 422; no unintended object |
| Packet baseline and filtering | Initially blocked; allowed only after filter; port change blocked old target |
| DNAT packet translation | WAN VIP reached LAN 8080, then 8081 after update |
| Hard stop of A | B took both VIPs in 5.01 seconds and served new connections |
| Write while A unavailable | Rejected `409 ha_unsafe` |
| Recovery of A after persisted configuration | Automatic MASTER/BACKUP recovery; updated target retained |
| Established TCP connection through failover | Same source port, zero reconnects; maximum observed response gap 3.83 seconds |
| Final DNAT removal | `peer_cleanup` acknowledged/applied/verified; absent on both nodes |
| Ports after deletion with B as MASTER | New connections to translated and direct paths rejected |
| Chromium against real lab firewalls | Alias, filter and DNAT create/edit/delete passed with peer verification |
| Mobile view, 390 px | No horizontal overflow, JavaScript errors or failed requests |

The test run records native responses, UUIDs, timing, packet nonces and cleanup outcomes. The browser CRUD result and production deployment smoke are recorded separately in the private audit bundle. No fixture result is presented as proof of real packet forwarding.

## Storage durability limit

An immediate hard stop after acknowledged updates lost recent changes on A, while B retained them. The restored system uses ZFS with a 90-second transaction interval; OPNsense's configuration save flushes its userspace buffer but does not call `fsync`. The first recovery required reconciliation. Subsequent power-loss tests used an explicit storage barrier on both nodes and recovered automatically. A successful plugin readback is not a guarantee against immediate power loss; the plugin does not change storage tuning or upstream code.

## Review and release decision

Codex identified the DNAT completeness issue, subsequently fixed. Fable's first retry was unavailable (429); its later completed review identified malformed upstream totals/CARP fields escaping after apply, a listing scale limitation and missing explicit evidence for final alias/filter deletion. Nine regression cases now preserve partial outcomes and audit (eight failed before the boundary fixes); the complete corrected suite passed 435 tests with 19 skipped, including 15 local Chromium E2E cases. The N+1 scale boundary is documented in `FIREWALL_MANAGEMENT.md`. The original real test already covered the sole API filter rule. A fresh isolated real run qualified the sole editable alias with explicit empty/single/empty inventories on both nodes.

**Final dual-family review completed:** Fable (`claude-fable-5`, actual CLI execution) and Codex independently reviewed the same 165,159-byte frozen artifact, SHA-256 `9758840de5ba70f966415f9c48e2164cdf0da13975406ad081b66893a384fb53`. Both returned **GO for the read-only production release**, with no remaining reproducible P0/P1/P2 in the verified CRUD path. The artifact includes payload input classes, audit and client exports omitted from the earlier Fable review. Runtime source is commit `e739d172acf67f0bd8c16f02fe25a58ad2560ae9`.

| Review finding | Source | Disposition |
|---|---|---|
| Synthetic DNAT lockout rows masked truncated inventory | Codex | Confirmed; count corrected and negative regression passed |
| Invalid totals / CARP numeric fields escaped after mutation | Fable | Confirmed; boundary errors now retain partial outcome and audit; negative regressions passed |
| Sequential N+1 listing and concurrent disappearance | Fable | Confirmed limitation; explicitly restricted qualification to small inventories, without claiming a performance fix |
| Last alias / filter deletion lacked explicit evidence | Fable | Evidence gap closed: filter already tested; sole editable alias retested on both real nodes |
| Suggested removal of `int(total)` ValueError handling as unreachable | Fable, optional P3 | Refuted: Python raises ValueError for a 4,301-digit numeric string at the default conversion limit; catch retained |

On the final shared artifact both reviewers reported zero new P0/P1/P2 (zero overlapping open findings). Earlier discovery counts are not a controlled overlap comparison because they involved successive artifacts. Optional cosmetic observations do not alter the release verdict.

Release may be deployed with production still read-only. Enabling production writes requires a separate operational decision and qualifying the actual pair and inventory. The lab result applies to the tested firmware, topology and synchronization mode; it does not qualify legacy writers or every firewall feature.

## Reproduction sequence

1. Restore and sanitize two appliances; assign distinct MAC, SSH and host identities. Create isolated LAN/WAN/sync networks and confirm there is no route to production.
2. Configure CARP on LAN/WAN, permit protocol 112, and configure pfsync plus one-way XMLRPC with only aliases/rules/NAT selected.
3. Use the plugin with `read_only:false` and `ha_sync_mode:xmlrpc_pf` only against that pair. Run alias, rule and DNAT CRUD; inspect both nodes and send independent packets.
4. Exercise stale revision, invalid upstream input, denied permission and split/missing peer states. Verify denied writes leave no object.
5. Flush storage, establish a TCP connection without automatic reconnect, hard-stop the primary, and verify both the existing connection and fresh connections through the backup.
6. Restore the primary; delete the objects, inspect both nodes, fail over again and verify fresh connections are blocked. Remove all test objects and stop the temporary appliances and access processes.

## Deployment and lab closure

Runtime commit `6e2007a526696760b2b834dd7ea630c1803c2a36` was pushed to canonical Gitea and confirmed on the GitHub mirror. The production installation matches all 48 packaged file hashes. Its configuration hash is unchanged, `read_only:true`, and the service is active with no recent tracebacks. Authenticated HTTP and Chromium smoke passed: the three management listings return 200, invalid POSTs return 403 read-only, and desktop/mobile UI has no JavaScript errors.

Both temporary VMs were shut down cleanly and retain a qualified snapshot. All test objects, access processes, namespaces, bridges and the lab-only host firewall table were removed. Host routes and the pre-existing firewall table set match the pre-lab baseline. The stopped VM disks remain available for requalification; credentials are encrypted in the private vault. The 1.15.2 alias follow-up temporarily recreated the same isolated networks, without any physical uplink or production route, and used only the lab pair.

The 1.15.2 follow-up is also closed: both VMs stopped cleanly, all temporary networks removed and host routes/other firewall tables preserved. Production now serves 1.15.2 from runtime commit `e739d17`, confirmed on canonical Gitea and the GitHub mirror. A fresh backup was extracted and checked before installation. All 48 deployed hashes match the package, configuration is unchanged and read-only, and the service is active. Authenticated listings returned 200 (7 DNAT, 2 filter, 22 alias entries); deliberately invalid POSTs returned 403 read-only. Chromium desktop/mobile passed without JavaScript errors. Logs contain no tracebacks since the deployment restart; the wider historical window contained pre-deployment Paramiko connection failures and is not described as error-free.
